// This file is kept as an empty placeholder to maintain file structure
// All toast functionality has been removed

// We're leaving this file as a placeholder since it may be imported elsewhere
// and removing it could cause import errors in files we can't modify
export const useToast = () => {
  return {};
};

export const toast = () => {
  return {};
};
